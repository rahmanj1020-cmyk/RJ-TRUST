const https = require('https');

// A script to wipe all data from DB since the user wants everything 000.
// Actually, it's easier to just reset the users, transactions, and adminWallet in memory or in DB.
